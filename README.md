# profile-card-docker-app
Containerized profile management app built with Node.js, MongoDB, and Docker. Features a profile card UI with image upload and real-time data updates. Uses Docker Compose to orchestrate three services — Node.js app (hosted on AWS ECR), MongoDB, and Mongo Express — with persistent volume storage.
